name: hosteday_flutter
description: A lightweight Flutter SDK for connecting apps with HosteDay APIs, including authentication, user requests, custom endpoints, and realtime support.
version: 1.0.5
homepage: https://hosteday.com

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.6.0
  meta: ^1.17.0
  dart_pusher_channels: ^1.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - hosteday
  - flutter
  - api
  - sdk
  - realtime

flutter:
